# server
server:
  port: 8080

spring:

  application:
    name: trackfolio

  # Database
  datasource:
    url: jdbc:postgresql://localhost:5432/trackfolio
    username: postgres
    password: adi
    driver-class-name: org.postgresql.Driver

  # Hibernate
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    open-in-view: false

    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

# JWT Secret
jwt:
  secret: eBw7TnIBDN+iD99ulb+eUAATpQU9YyV5I1imRo0vCnk=